apiVersion: v1
kind: Secret
metadata:
  name: status-aggregator
  namespace: status-aggregator
type: Opaque
stringData:
  # Placeholders — GitHub and Sentry adapters are disabled via ENABLE_*
  # env flags in the Deployment. Populate these locally only if you want
  # to exercise the live adapters against real tokens.
  GITHUB_APP_ID: ""
  GITHUB_APP_INSTALLATION_ID: ""
  GITHUB_APP_PRIVATE_KEY: ""
  SENTRY_API_TOKEN: ""
  SENTRY_DSN: ""
